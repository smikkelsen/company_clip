# ============== GEM SOURCE ===============
source 'https://rubygems.org'
source 'http://myebiz:myeb1z@gems.myebiz.com'

# ============== FRAMEWORK ================
gem 'rails', '3.2.13'
# ============== DATABASE =================
gem 'mysql2'

# =============== ADMIN ===================

# ========== AUTHENTICATION ===============
gem 'encryption_client', '>= 0.0.1'

# =============== OTHER ===================
gem 'nested_form'

# ========= BETA / PRODUCTION =============

# =========== ASSETS / VIEWS ==============
gem 'jquery-rails', '2.2.1'

group :assets do
  gem 'sass-rails', '~> 3.2.3'
  gem 'coffee-rails', '~> 3.2.1'
  gem 'uglifier', '>= 1.0.3'
  gem 'chosen-rails'
end

# ============ DEVELOPMENT ================
group :development do
  gem 'better_errors'
  gem 'binding_of_caller'
  gem 'meta_request'
  gem 'sqlite3'
end

# =============== TEST ====================
group :test do
  #gem 'ZenTest', '4.8.3'
  #gem 'rspec-rails'
  #gem 'shoulda-matchers'
  #gem 'factory_girl_rails'
  #gem 'autotest-rails'
  #gem 'faker'

  ## ---- GUARD ----
  #gem 'guard-rspec'
  #gem 'guard-livereload' # live reload browser - requires extension:
  ##gem 'rack-livereload' # live reload middleware for browsers
  ## in development.rb - config.middleware.insert_after(ActionDispatch::Static, Rack::LiveReload)
  ## os notification protocols
  #gem 'rb-inotify', :require => false
  #gem 'rb-fsevent', :require => false
  #gem 'rb-fchange', :require => false

end